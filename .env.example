# Employee Attendance System - Environment Configuration

# ============================================
# BACKEND CONFIGURATION
# ============================================

# Backend server port
BACKEND_PORT=4000

# JWT token configuration
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=1h

# Bcrypt password hashing (10-12 recommended)
BCRYPT_SALT_ROUNDS=10

# Node environment
NODE_ENV=development

# Frontend URL (for CORS if needed)
FRONTEND_URL=http://localhost:3000

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Backend API base URL (used by React app)
# For local development: http://localhost:4000
# For Docker Compose: http://backend:4000 (service name)
# For production: https://api.yourdomain.com
VITE_API_BASE=http://localhost:4000

# Frontend port (Vite dev server)
FRONTEND_PORT=5173

# ============================================
# DOCKER COMPOSE CONFIGURATION
# ============================================

# Docker Compose will use these to build and run containers
# Change these values if you need different ports
COMPOSE_PROJECT_NAME=employee-attendance-system
